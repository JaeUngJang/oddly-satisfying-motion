## Unit

- id:
- one line:

## Checks

- [ ] `xcodebuild test -scheme WowUnits` passes (contract tests)
- [ ] `portability/run.sh` reports SUCCESS for all hosts with 0 warnings
- [ ] Reduce Motion fallback preserves meaning
- [ ] Works with `WowSettings.hapticsDisabled = true`
- [ ] `unit.json` spec matches `static let spec`
